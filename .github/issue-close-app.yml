# Comment that will be sent if an issue is judged to be closed.
comment: "This issue is closed because it does not meet our issue template. Please follow the initial template that appears when you open a new issue. Your submission should at a minimum include at least the words 'NAME' if submitting an email request, 'Description' for a bug, or 'Feature' for a feature request."
issueConfigs:
# There can be several configs for different kind of issues.
- content:
# bug report
  - "Description"
- content:
# email add request 
  - "NAME"
- content:
# feature 
  - "Feature"
# The issue is judged to be legal if it includes all keywords from any of these two configs.
# Or it will be closed by the app

# Optional configuration:
#
# whether the keywords are case-insensitive
# default value is false, which means keywords are case-sensitive
caseInsensitive: true

# the label that will be added when the bot close an issue
# The bot will only add a label if this property is set.
#label: "closed by bot"

# The bot will ignore any issues that are opened or reopened by the user names in exception
#exception:
#  - "username1"
#  - "username2"
